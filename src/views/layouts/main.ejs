<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>LeChung Tech | Đỉnh cao công nghệ</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet">
    <style>body { font-family: 'Inter', sans-serif; }</style>
</head>
<body class="bg-slate-50 min-h-screen">
    <nav class="sticky top-0 z-50 bg-white/80 backdrop-blur-md border-b border-slate-200">
        <div class="container mx-auto px-6 py-4 flex justify-between items-center">
            <a href="/products" class="text-2xl font-black tracking-tighter text-indigo-600">
                LECHUNG <span class="text-slate-800">TECH</span>
            </a>

            <div class="flex items-center space-x-8">
                <div class="hidden md:flex space-x-6 font-medium text-slate-600">
                    <a href="/products" class="hover:text-indigo-600 transition">Sản phẩm</a>
                    <a href="#" class="hover:text-indigo-600 transition">Khuyến mãi</a>
                </div>

                <div class="flex items-center space-x-4 border-l pl-8 border-slate-200">
                    <% if (user) { %>
                        <span class="text-slate-700 font-semibold">
                            <%= user.username %>
                        </span>
                        <a href="/logout" class="text-sm bg-slate-100 text-slate-600 px-4 py-2 rounded-full hover:bg-red-50 hover:text-red-600 transition font-medium">
                            Đăng xuất
                        </a>
                    <% } else { %>
                        <a href="/login" class="text-slate-600 font-medium hover:text-indigo-600">Đăng nhập</a>
                        <a href="/register" class="bg-indigo-600 text-white px-5 py-2 rounded-full font-medium hover:bg-indigo-700 shadow-lg shadow-indigo-200 transition">
                            Đăng ký
                        </a>
                    <% } %>
                </div>
            </div>
        </div>
    </nav>

    <main class="container mx-auto py-10 px-6">
        <%- body %>
    </main>
</body>
</html>