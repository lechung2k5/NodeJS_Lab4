<div class="max-w-md mx-auto py-20">
    <div class="bg-white rounded-[2.5rem] shadow-2xl p-12 border border-slate-100">
        <div class="text-center mb-10">
            <h1 class="text-3xl font-black text-slate-800">Mừng bạn trở lại!</h1>
            <p class="text-slate-400 mt-2 font-medium">Đăng nhập để trải nghiệm LeChung Tech</p>
        </div>

        <% if (error) { %>
            <div class="bg-red-50 text-red-600 p-4 rounded-2xl mb-8 text-sm font-medium border border-red-100 text-center">
                <%= error %>
            </div>
        <% } %>

        <form action="/login" method="POST" class="space-y-6">
            <div class="space-y-2">
                <label class="text-sm font-bold text-slate-700 ml-1">Tài khoản</label>
                <input type="text" name="username" required class="w-full px-5 py-4 bg-slate-50 border-none rounded-2xl focus:ring-2 focus:ring-indigo-500 outline-none transition shadow-inner">
            </div>
            <div class="space-y-2">
                <label class="text-sm font-bold text-slate-700 ml-1">Mật khẩu</label>
                <input type="password" name="password" required class="w-full px-5 py-4 bg-slate-50 border-none rounded-2xl focus:ring-2 focus:ring-indigo-500 outline-none transition shadow-inner">
            </div>
            <button type="submit" class="w-full bg-indigo-600 text-white py-4 rounded-2xl font-bold shadow-xl shadow-indigo-100 hover:bg-indigo-700 hover:-translate-y-1 transition duration-200">
                Đăng nhập ngay
            </button>
        </form>
        
        <p class="text-center mt-8 text-slate-500">
            Chưa có tài khoản? <a href="/register" class="text-indigo-600 font-bold hover:underline">Đăng ký</a>
        </p>
    </div>
</div>